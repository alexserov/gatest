name: Comments test

concurrency:
  group: wf-${{github.event.pull_request.number}}-${{github.workflow}}
  cancel-in-progress: true

on:
  pull_request:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build
    timeout-minutes: 30
    
    steps:

    - name: Check comments
      id: wait-for-test
      if: ${{ github.event_name == 'pull_request' && contains(toJSON(github.event.labels.*.name), 'force_test') }}
      run: echo Hello!

    # - uses: actions/upload-artifact@v2
    #   with:
    #     name: my-artifact
    #     path: log.txt
